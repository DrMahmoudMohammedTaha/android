<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aboahmed.e008"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="14" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
//LANUCHER is the catagory for the first activity work in the program
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
   <activity android:name="second">
    		<intent-filter>
    		    //this action tag is the action value we set to make any intent to go to this activity 
    		    <action android:name="android.intent.action.second" />
				//we can add any catagory and you can name it
				//we can add more than one catagory in the same activity to differantiaiate between activities with the same action
                <category android:name="android.intent.category.DEFAULT" />
    		    
    		    
    		    		    </intent-filter>	 
    </activity>
    </application>

</manifest>
